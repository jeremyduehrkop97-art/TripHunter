"""Real dispatch channels for formatted alerts - currently just Telegram.
See telegram.py. A dispatcher sends an already-formatted message
(alerts/instant_alert_formatter.py); it never formats one itself.
"""
